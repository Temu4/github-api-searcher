const BASE_API_URL = 'https://api.github.com';

export const SEARCH_USERS_URL = `${BASE_API_URL}/search/users`; // + '?q=<query>'

export const USER_URL = `${BASE_API_URL}/users/`; // + '<username>'

export const USER_REPOS_URL = `${BASE_API_URL}/users/`; // + '<username>/repos'
